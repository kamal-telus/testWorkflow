name: First push to main workflow
on: 
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  
jobs: 
  first-job:
   runs-on: ubuntu-latest
   steps:
     - name: Print greeting
       run: echo 'Hello world!'
     - name: Print goodbye
       run: |
         echo 'Done 1  -  bye!'
         echo 'Done  -  bye!'
